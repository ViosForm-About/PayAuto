import { useState } from "react"
import AmountInput from "./AmountInput"
import QrisButton from "./QrisButton"

export default function PaymentCard() {
  const [amount, setAmount] = useState("")

  return (
    <div style={{marginTop:20}}>
      <AmountInput amount={amount} setAmount={setAmount} />
      <QrisButton amount={amount} />

      <div className="method">🏦 BCA 123456789 a/n VIOS</div>
      <div className="method">📱 Dana 08123456789</div>
      <div className="method">🟣 OVO 08123456789</div>
      <div className="method">🟢 GoPay 08123456789</div>
    </div>
  )
}